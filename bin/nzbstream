#!/usr/bin/env python
import logging
import sys

if __name__ == "__main__":
  from nzbstream import cmdline

  logging.basicConfig(filename="nzbstream.log", level=logging.DEBUG, format="[%(levelname)-8s] %(name)-20s: %(message)s", filemode="w")
  cmdline.run()
